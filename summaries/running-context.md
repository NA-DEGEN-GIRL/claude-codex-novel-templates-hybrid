# 롤링 컨텍스트

> 이 파일은 200줄 이내로 유지한다. 매 화 집필 후 갱신.

<!--
  === compile_brief 헤딩/포맷 계약 (파서가 이 형식을 기대한다) ===

  compile_brief는 아래 `## ...` 헤딩을 이름으로 찾는다 (한글/영문 alias 지원).

  - `## 현재 상태` (또는 Current Status/Current State) — 위치/시간/긴장 등 현재 스냅샷.
  - `## Immediate Carry-Forward` (또는 `## 직전 화 직결 상태` / `## Carry-Forward`)
      — 다음 화 오프닝에 반드시 이어질 사실 3~7개 bullet. 브리프 Live Cues의 핵심 앵커.
        이 섹션이 비면 브리프에 `⚠️ PARSE-MISS: running-context 직결 상태`가 뜬다.
  - `## 압축 흐름` (또는 전체 흐름 압축 / Recent Events / Compressed Flow) — 최근 2아크 요약.
  - `## 엔딩 훅 추적` — 표: `| 화 | 훅 타입 | 요약 |` (최근 5~7행).
  - `## HOLD 경고` — 열린 HOLD를 `HOLD-001: ...`처럼 1줄씩. 없으면 비워 둔다.
  - (선택) `## 캐릭터 최종 상태`, `## 복선 최종 상태` 표도 있으면 등장인물 기준으로 추출한다.
-->

## 현재 상태

- **최신 집필**: (없음)
- **다음 집필**: (미정)

## Immediate Carry-Forward

> 다음 화 오프닝에 반드시 이어져야 하는 사실만 3~7개 bullet로 유지한다.
> 위치/시간, 부상·자원 상태, 공개 정보와 비공개 정보, 이미 처리된 일과 아직 안 된 일을 적는다.

(집필 시작 후 기록)

## 압축 흐름

(집필 시작 후 기록)

## 최근 에피소드 상세

(최근 5~6화 상세. 오래된 에피소드는 압축 흐름으로 이관)

## 엔딩 훅 추적 (최근 5화)

| 화 | 훅 타입 | 요약 |
|----|---------|------|

## HOLD 경고

(현재 열려 있는 HOLD가 없으면 비워 둔다. 있으면 `HOLD-001: 16화 전 해소 필요`처럼 1줄씩 적는다.)

## 다음 화 미리보기

(다음 화 예고)
